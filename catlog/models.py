from django.db import models

# Create your models here.


class Catlog_images(models.Model):
    catlog_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Food(models.Model):
    food_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Art(models.Model):
    art_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Business(models.Model):
    business_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Spring(models.Model):
    spring_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Fashion(models.Model):
    fashion_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Retail(models.Model):
    retail_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Backgrounds(models.Model):
    backgrounds_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Landscape(models.Model):
    landscape_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Urbanlife(models.Model):
    urbanlife_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Work(models.Model):
    work_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Home(models.Model):
    home_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Accessories(models.Model):
    accessories_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class People(models.Model):
    people_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Flowers(models.Model):
    flowers_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Beauty(models.Model):
    beauty_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Money(models.Model):
    money_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Holidays(models.Model):
    holidays_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Jewlery(models.Model):
    jewlery_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Transportation(models.Model):
    transportation_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Nature(models.Model):
    nature_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Fitness(models.Model):
    fitness_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Education(models.Model):
    education_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Cinema(models.Model):
    cinema_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Health(models.Model):
    health_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Love(models.Model):
    love_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class World(models.Model):
    world_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Technology(models.Model):
    technology_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Celebrate(models.Model):
    celebrate_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Travel(models.Model):
    travel_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Communication(models.Model):
    communication_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Recreation(models.Model):
    recreation_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Children(models.Model):
    children_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class City(models.Model):
    city_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Seasons(models.Model):
    seasons_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)


class Wellness(models.Model):
    wellness_1 = models.TextField(max_length=300, default='')

    def __str__(self):
        template = '{0.id}'
        return template.format(self)

